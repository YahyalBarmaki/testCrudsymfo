{{ form_start(form) }}
    <div class="form-group">
        {{ form_widget(form) }}
        <br>
        <button class="btn btn-secondary  ">{{ button_label|default('Ajouter') }}</button>
    </div>
{{ form_end(form) }}
